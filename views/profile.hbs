  
{{> (lookup . 'navbarPartial')}}

<div class="container">

    <h1>Esto es el perfil de: {{user.username}} </h1>
    <img src="{{user.profileImg}}" alt="profileImg">



    {{#if userRegistered}}
        <p>Esto solo lo puede ver el usuario logueado: {{user.username}}</p>
        <p>Y esto tambien</p>
    {{/if }}

</div>

